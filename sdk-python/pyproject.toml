[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stimpact-sdk"
version = "0.3.0"
description = "Stimpact Python telemetry SDK"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Stimpact" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
