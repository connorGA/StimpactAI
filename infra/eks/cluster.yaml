apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: stimpactai-cluster
  region: us-west-2
  version: "1.35"

autoModeConfig:
  enabled: false

iam:
  withOIDC: true

cloudWatch:
  clusterLogging:
    enableTypes:
      - api
      - audit

managedNodeGroups:
  - name: control-plane-ng
    instanceType: t3.medium
    desiredCapacity: 1
    minSize: 1
    maxSize: 2
    labels:
      stimpact.ai/workload: control-plane
    tags:
      workload: control-plane
      managed-by: stimpact

  - name: sandbox-ng
    instanceType: t3.large
    desiredCapacity: 0
    minSize: 0
    maxSize: 4
    labels:
      stimpact.ai/workload: sandbox
    taints:
      - key: stimpact.ai/workload
        value: sandbox
        effect: NoSchedule
    tags:
      workload: sandbox
      managed-by: stimpact
